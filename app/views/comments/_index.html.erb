<div>
  <% @comments.sort_by(&:created_at).each do |comment| %>
    <p>
      <%= comment.content %>
      <strong><%= comment.user.first_name + " " + comment.user.last_name %></strong>
        <% if current_user.id == comment.user_id%>
          <%= link_to "Editer", edit_gossip_comment_path(@gossip.id, comment.id), class:"btn btn-outline-success" %>
          <%= link_to "Supprimer", gossip_comment_path(@gossip.id, comment.id), class:"btn btn-outline-danger", data:{confirm:"Etes vous sûr de vouloir supprimer ce potin ?", method:"DELETE"} %>
        <% end %>
    </p>
    <% end %>
</div>
